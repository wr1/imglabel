

# todo list
- migrate from pyqt to pyside due to licensing
- make the cli the entry point, also for the gui
    - imglabel batch <yaml> <images>
    - imglabe gui <imagesordirs>
- implement datamodels (add pydantic dep) for the yaml output from the gui (filter properties) and for imglabel batch json output (labelme)
- add programmatic example for the batch labelling workflow (use the examples/image.jpg image)
